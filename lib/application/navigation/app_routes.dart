/// Root paths for [GetMaterialApp].
abstract final class AppRoutes {
  /// Marketing landing page (first screen for new visitors).
  static const landing = '/';

  /// Signed-in / main product shell (dashboard).
  static const dashboard = '/app';
}
